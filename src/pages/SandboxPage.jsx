import Sandbox from '@/features/sandbox-page/Sandbox';

/**
 * 🗺️ ROTA ENXUTA / PASSA-PRATO: SandboxPage
 * Substitui o formulário antigo de testes pelo laboratório comparativo modular.
 * Transfere toda a responsabilidade de visualização para o recurso isolado em features.
 */
export default function SandboxPage() {
  return <Sandbox />;
}
